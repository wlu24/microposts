// the escape_javascript method is needed to escape out the result when
//inserting HTML in a JavaScript file.

$("#follow_form").html("<%= escape_javascript(render('users/follow')) %>");
$("#followers").html('<%= @user.followers.count %>');
