<% if @feed_items.any? %>
  <ol class="microposts">

    <%# Here Rails knows to call the micropost partial because each element %>
    <%# of @feed_items has class Micropost. This causes Rails to look for %>
    <%# a partial with the corresponding name in the views directory of the %>
    <%# given resource, namely: app/views/microposts/_micropost.html.erb %>
    <%= render @feed_items %>

  </ol>
  <%= will_paginate @feed_items %>
<% end %>
